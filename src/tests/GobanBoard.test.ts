export {}

describe("GobanBoard capture logic", () => {
  it("shows that the white stone is captured when surrounded", () => {
    // Render the GobanBoard
    // Click the middle stone (1, 1)
    // Assert that the result says: captured
  })

  it("shows that the white stone is not captured when it has a liberty", () => {
    // Modify the board so there is a liberty
    // Click the white stone
    // Assert that the result says: not captured
  })
})
